
    # def openFile(self):
    #      directory = QtWidgets.QFileDialog.getOpenFileName(self, "Abrir Arquivo" ,'C:\PC\Desktop', "Arquivos de Texto (*.png)")[0]
    #      with open(directory) as a:
    #          myList = [ ''.join(line.split()) for line in a ]
    #          print(myList)
    #          #self.combo_modelo.addItems(myList)

    # def read_folder(self):
    #     arquivo = sorted(glob(r'../GIT_PDV/resized/*.png'))
    #     img = Image.open(arquivo)
    #     qimg = ImageQt(img)
    #     pixmap = QtGui.QPixmap.fromImage(qimg)
    #     self.img.setPixmap(pixmap)
    
    # def openFile(self):
    #     directory = QtWidgets.QFileDialog.getOpenFileName(self, "Abrir Arquivo" ,'C:\PC\Desktop', "Arquivos de Texto (*.png)")[0]
    #     with open(directory, 'r') as a:
    #         for line in a:
    #             myList = []
    #             myList.append(line)
    #             print(myList)
        #self.produto.setPixmap(directory)
            #self.combo_modelo.addItems(myList)
#
# declaracao = f"""insert into orcamento
#                             (produto, quantidade, altura, largura)
#                             values
#                             ('{produto}', '{quantidade}', ' {altura}', ' {largura})'"""
#         print(declaracao)